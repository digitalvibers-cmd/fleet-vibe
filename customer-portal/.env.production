FLEETBASE_API_URL=https://api.flybox.rs
NEXT_PUBLIC_PORTAL_URL=https://flybox.rs
# Real value injected at build time via docker-compose.<env>.yml build.args (server .env).
# Client-side key — must be HTTP-referrer restricted in Google Cloud Console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
